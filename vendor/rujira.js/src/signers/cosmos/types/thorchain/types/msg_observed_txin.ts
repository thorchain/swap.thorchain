// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: types/msg_observed_txin.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "../../binary";
import {
  base64FromBytes,
  bytesFromBase64,
  DeepPartial,
  Exact,
  isSet,
} from "../../helpers";
import { ObservedTx } from "./type_observed_tx";

export const protobufPackage = "types";

export interface MsgObservedTxIn {
  txs: ObservedTx[];
  signer: Uint8Array;
}

function createBaseMsgObservedTxIn(): MsgObservedTxIn {
  return { txs: [], signer: new Uint8Array(0) };
}

export const MsgObservedTxIn = {
  encode(
    message: MsgObservedTxIn,
    writer: BinaryWriter = BinaryWriter.create()
  ): BinaryWriter {
    for (const v of message.txs) {
      ObservedTx.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    if (message.signer.length !== 0) {
      writer.uint32(18).bytes(message.signer);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgObservedTxIn {
    const reader =
      input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgObservedTxIn();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.txs.push(ObservedTx.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.signer = reader.bytes();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgObservedTxIn {
    return {
      txs: globalThis.Array.isArray(object?.txs)
        ? object.txs.map((e: any) => ObservedTx.fromJSON(e))
        : [],
      signer: isSet(object.signer)
        ? bytesFromBase64(object.signer)
        : new Uint8Array(0),
    };
  },

  toJSON(message: MsgObservedTxIn): unknown {
    const obj: any = {};
    if (message.txs?.length) {
      obj.txs = message.txs.map((e) => ObservedTx.toJSON(e));
    }
    if (message.signer.length !== 0) {
      obj.signer = base64FromBytes(message.signer);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgObservedTxIn>, I>>(
    base?: I
  ): MsgObservedTxIn {
    return MsgObservedTxIn.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgObservedTxIn>, I>>(
    object: I
  ): MsgObservedTxIn {
    const message = createBaseMsgObservedTxIn();
    message.txs = object.txs?.map((e) => ObservedTx.fromPartial(e)) || [];
    message.signer = object.signer ?? new Uint8Array(0);
    return message;
  },
};
